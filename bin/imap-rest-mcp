#!/usr/bin/env node
'use strict';

const { main } = require('../src/mcp/index.js');

main().catch((err) => {
    process.stderr.write(`imap-rest-mcp failed to start: ${err.message}\n`);
    process.exit(1);
});
